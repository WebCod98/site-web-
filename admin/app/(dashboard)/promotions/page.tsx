import PromotionsView from './PromotionsView';

/** SCULPT'AURA Admin — promotions route (promo codes + affiliate tracking). */
export default function PromotionsPage() {
  return <PromotionsView />;
}
